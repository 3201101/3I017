/**
 * 
 */
function main(id, login, clef) {
	environnement = {};
	environnement.users = [];
}

function user(id, login){
	this.id = id;
	this.login = login;
	}
}

function connect(login, password){
	
}